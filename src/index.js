const init = () => {
  
}


// //Add event listener which waits for Dom to load.  upon loading, it calls function 2
document.addEventListener('DOMContentLoaded', init);









// function 2 listens to submit event, and when it fires, it grabs the input and stores it in a variable
// it also prevents page from reloading
// it calls JSON lookup function and sends ID variable

// function JSON lookup
// fetches JSON, converts Data, then searches through looking for correct ID variable

// Function
// add new text to website display 
